@prefix schema: <http://schema.org/>.

<#note>
    a schema:TextDigitalDocument;
    schema:name "Shared Note";
    schema:text "This note is readable by any client as long as the requester is the pod owner.".
